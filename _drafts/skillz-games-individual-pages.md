<a href="{{ game.page-link }}" alt="More Info" title="What is {{ game.name }}">
<img src="{{ game.image-path }}" alt="{{ game.image-alt }}" title="{{ game.name }}">
<h3>{{ game.name }}</h3>
{% include icons/info.html %}
</a>
